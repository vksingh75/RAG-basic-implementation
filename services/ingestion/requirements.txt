# Ingestion service dependencies — versions verified against PyPI 2026-07-04.
# torch/torchvision are installed CPU-only in the Dockerfile BEFORE this file, via
#   --extra-index-url https://download.pytorch.org/whl/cpu (torch==2.12.1+cpu).
# The pins below keep a bare `pip install -r requirements.txt` compatible with
# sentence-transformers (torch>=1.11) and docling (torch>=2.2.2,<3).
fastapi==0.139.0
uvicorn[standard]==0.50.0
python-multipart==0.0.32
pydantic>=2,<3
pydantic-settings==2.14.2
docling==2.109.0
docling-core[chunking]==2.86.0
sentence-transformers==5.6.0
qdrant-client==1.18.0
psycopg[binary,pool]==3.3.4
torch>=2.2.2,<3
